<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anilreddy.unit_testing_android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.main.ShowTextActivity" />
        <activity android:name=".ui.custommatcher.CustomMatcher" />
        <activity android:name=".ui.longlist.LongListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.resourcesample.ResourceSample" />
        <activity android:name=".ui.imageview.ImageViewerActivity" />
        <activity android:name=".ui.intentsample.ContactsActivity" />
        <activity android:name=".ui.intentsample.DialerActivity" />
        <activity android:name=".unit.basicsample.TestMainActivity"></activity>
    </application>

</manifest>